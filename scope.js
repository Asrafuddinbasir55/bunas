const baperTaka = 10000;
function fatriCi(age, earning) {
    // console.log(tureAge);
    var dicition = canMarry(1500)
    console.log(dicition);
    console.log(age);
    console.log(earning);
    const tureAge = + 7
     function canMarry(expence) {
        const remaning = baperTaka + earning - expence;
        
        if (remaning > 1000) {
            const swoning = 500
            console.log(swoning);
           return true
        }
        return false
    }
}

fatriCi(21, 5000)